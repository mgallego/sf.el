The instalation of sf.el is very simple.

1. Make sure sf.el is in your load-path (stick it in .emacs.d or whatnot)

2. (require 'sf) in your .emacs


Thats is all

You can view the atual functions of the library in HOWTO doc in the repository.
